import Blogs from "../components/blogComponents/Blogs"; // Ensure the Blogs component is correctly imported

const BlogsPage = () => {
  return <Blogs />; // Render the Blogs component
};

export default BlogsPage;
